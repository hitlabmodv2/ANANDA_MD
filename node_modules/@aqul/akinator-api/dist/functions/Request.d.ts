import { region } from "../constants/Config";
import { ResponseSetupAki } from "../types/Aki";
export declare const setupAki: (region: region, childMode: boolean, config?: any) => Promise<ResponseSetupAki>;
export declare const requestAki: <T>(url: string, body: any, config?: any) => Promise<T>;
